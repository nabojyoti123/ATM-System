Create the basic HTML skeleton
Link the CSS file
Link the JS file

Create a main container div (to hold the ATM)
    Display Title: "ATM System" (h1)
    Display Balance: "Balance: ₹5000" (p)
    Create input field: User types amount here (input)
    
    Create a container for buttons
        Button: Deposit
        Button: Withdraw
        Button: Check Balance
        Button: Exit
        
    Display Message Area: "Result Message Here" (p)
    Step 3: Add Basic CSS (Styling)
We want this to look like a clean ATM screen. Copy this into your style.css file.
Step 4: JavaScript Logic
Since you haven't fully learned DOM manipulation yet, I am giving you the exact bridge code (document.getElementById) needed to connect your JS logic to the HTML
